# A CSC222 ncurses clock project!  
## Note: Commits may appear to be from a single user, as we are using Live Share to work collaboratively in real time.  
Run ```g++ FractionUI.cpp -o ./build/fractionui -lncurses``` to compile. (delete this line when done)  
